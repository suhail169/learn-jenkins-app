pipeline {
    agent any
    
    stages {

        stage('Buid Docker image'){
            steps{
                sh 'docker build -f ci/dockerfile-playwright -t my_playwright .'
            }
        }
    }
}